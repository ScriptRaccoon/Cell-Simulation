// import { population } from "../myPopulation.js";

// export const stop = (type) => (body) => {
//     for (const other of population.get(type)) {
//         if (body.touches(other)) {
//             other.maxSpeed = 0;
//             other.maxForce = 0;
//         }
//     }
// };
